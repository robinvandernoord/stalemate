github_username = "robinvandernoord"
max_age_days = 180

# Filter for Python repos only (checks for pyproject.toml)
python_only = true

# Check if repos support the latest Python version
check_python_version = true

# Optional: repos to skip (e.g., forks you maintain, test repos)
exclude_repos = []

# Optional: skip fork repos
exclude_forks = true
